import database from '../firebase/firebase';

// Set up initial list of categories
const categoriesAdd = () => {
  console.log('Start add of default categories - categoriesAdd.js');
  // database.ref(`/categories`).push({category: 'Operators'}).then(null, console.log);
  // database.ref(`/categories`).push({category: 'Expressions'}).then(null, console.log);
  // database.ref(`/categories`).push({category: 'Values & Types'}).then(null, console.log);
  // database.ref(`/categories`).push({category: 'Variables'}).then(null, console.log);
  // database.ref(`/categories`).push({category: 'Blocks'}).then(null, console.log);
  // database.ref(`/categories`).push({category: 'Conditionals'}).then(null, console.log);
  // database.ref(`/categories`).push({category: 'Loops'}).then(null, console.log);
}

export default categoriesAdd;